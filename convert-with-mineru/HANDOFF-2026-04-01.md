# MineU Skill Session Handoff

## Summary

- Scope: MineU skill output contract fixes and final hardening.
- Skill root: `C:\Users\zengw\OneDrive\Files\SelfMadeSkills\convert-with-mineru`
- Main outcome: source-named Markdown, image folder, and typed JSON folder outputs are now implemented and validated.

## User Requests Completed

1. Fixed broken image references in renamed `.md` outputs.
2. Changed JSON outputs from a single `source.json` file to a `source.json/` directory with typed files.
3. Removed `raw` output retention and stopped preserving parsed source-file copies.
4. Prioritized Markdown as the main artifact while organizing JSON outputs under `source.json/`.
5. Fixed recursive discovery so existing MineU output trees are not reprocessed.
6. Fixed repeated same-output-root JSON staleness by clearing stale JSON directories before writing new artifacts.
7. Added a Windows/OneDrive-safe fallback for JSON directory cleanup when `shutil.rmtree()` hits transient `PermissionError`.

## Current Output Contract

For `source.pdf`, precision output now follows this shape:

- `source.md`
- `source.images/`
- `source.json/`
  - `source.content_list.json`
  - `source.content_list_v2.json`
  - `source.layout.json`
  - `source.model.json`

Notes:

- Only JSON artifact types actually produced by MineU are written.
- `source.raw/` is no longer written.
- Parsed source-file copies are no longer retained.

## Key Code Changes

### `scripts/mineru_outputs.py`

- `OutputTargets` now models:
  - `markdown`
  - `images_dir`
  - `json_dir`
  - `json_files`
- Output mapping now targets `source.json/` plus `source.<type>.json` files.

### `scripts/mineru_precision.py`

- Rewrites Markdown image links from `images/...` to `source.images/...`.
- Extracts typed JSON artifacts into `source.json/`.
- Removes raw output preservation.
- Clears stale JSON directories before writing fresh JSON.
- Adds retry logic for JSON-dir deletion.
- Adds Windows PowerShell fallback if repeated `shutil.rmtree()` attempts fail on OneDrive/Windows locks.

### `scripts/mineru_inputs.py`

- Recursive discovery now excludes:
  - the current configured output root
  - parent directories starting with `_mineru`
  - directories ending in `.images`, `.json`, `.raw`

This prevents previously generated MineU outputs from being re-ingested as new inputs.

### `scripts/mineru_convert.py`

- Precision path is fixed to `keep_raw_tree=False`.
- CLI output now prints each JSON artifact path that actually exists.

### Documentation Updated

- `SKILL.md`
- `references/configuration.md`

These now match the implemented output contract, including `content_list_v2`.

## Validation Performed

### Local Validation

- `pytest tests -q` -> `27 passed`
- `lsp_diagnostics` for `scripts/` -> `0`
- `lsp_diagnostics` for `tests/` -> `0`

### Real MineU Validation

#### Batch validation

- Real MineU API batch run against user-provided document set confirmed:
  - source-named `.md`
  - source-named `.images/`
  - source-named `.json/` with typed JSON files
  - no `raw/`

#### Same-output-root repeat validation

- Local real API sample used:
  - `C:\Users\zengw\OneDrive\Files\SelfMadeSkills\convert-with-mineru\live-repeat-sample.png`
- Output root used:
  - `C:\Users\zengw\OneDrive\Files\SelfMadeSkills\convert-with-mineru\live-repeat-output`
- First real pass wrote:
  - `live-repeat-sample.md`
  - `live-repeat-sample.json\live-repeat-sample.content_list.json`
  - `live-repeat-sample.json\live-repeat-sample.content_list_v2.json`
  - `live-repeat-sample.json\live-repeat-sample.layout.json`
  - `live-repeat-sample.json\live-repeat-sample.model.json`
- Then stale files were injected manually:
  - `live-repeat-sample.legacy.json`
  - overwritten stale `live-repeat-sample.content_list_v2.json`
- Second real pass to the same output root succeeded.
- Final read confirmed:
  - `legacy.json` was gone
  - only the 4 expected JSON files remained
  - `content_list_v2.json` contained real MineU output, not the stale override

## Oracle / Review Outcomes

- Output-contract review concluded the main contract is correctly implemented.
- Residual repeated-output-root stale-JSON risk was initially identified, then addressed.
- Final conservative conclusion:
  - the identified stale JSON directory risk is closed
  - no stronger claim was made about every conceivable repeated-output-root scenario

## Remaining Notes

- The shell session used during work could not see `E:\` later in the session, even though earlier tool reads could. This was treated as an environment visibility issue, not a code bug.
- A local real MineU sample under the skill directory was used to finish same-output-root verification.

## Sensitive Data Note

- The user pasted a real MineU token earlier in the conversation.
- It was written to `C:\Users\zengw\.config\opencode\local\mineru.env` for validation.
- Recommended follow-up: rotate that token.

## Important Paths

- Skill root:
  - `C:\Users\zengw\OneDrive\Files\SelfMadeSkills\convert-with-mineru`
- Local config used for validation:
  - `C:\Users\zengw\.config\opencode\local\mineru.env`
- Same-output-root repeat validation output:
  - `C:\Users\zengw\OneDrive\Files\SelfMadeSkills\convert-with-mineru\live-repeat-output`

## Suggested Next Actions

1. Rotate the MineU token.
2. If desired, migrate the skill into the OpenCode skills directory.
3. After migration, run one smoke test with a local sample to confirm environment parity.
